#pragma once

#include <string>

namespace Constants
{
	const std::string DEFAULT_FONT{ "fonts/Nanum.ttf" };
	const int DEFAULT_FONT_SIZE = 24;
	const int USER_NAME_FONT_SIZE = 12;
	const int USER_NAME_POS_DELTA = 15;
}
